{
  "name": "web-backend-asteroids",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "infra:up": "docker compose up -d postgres adminer minio minio-setup",
    "minio:up": "docker compose up -d minio minio-setup",
    "minio:logs": "docker compose logs -f minio minio-setup",
    "start": "node dist/main.js",
    "start:dev": "nest start --watch",
    "check": "nest build"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.6",
    "@nestjs/core": "^11.1.6",
    "@nestjs/platform-express": "^11.1.6",
    "@nestjs/typeorm": "^12.0.1",
    "pg": "^8.23.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "typeorm": "^1.1.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.10",
    "@types/node": "^24.3.0",
    "ffmpeg-static": "^5.3.0",
    "typescript": "^5.9.2"
  }
}
